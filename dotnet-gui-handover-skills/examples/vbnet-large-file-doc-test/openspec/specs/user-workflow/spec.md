# User Workflow Specification

## Requirements

### Requirement: Workflow chunk Form: MainForm shall be reviewable

The system SHALL make `Form: MainForm` available as a workflow or UI-behavior review unit.

#### Scenario: Workflow chunk Form: MainForm shall be reviewable

- GIVEN a maintainer reviews GUI behavior
- THEN chunk `MainForm` MUST provide source references and related chunks.

### Requirement: Workflow chunk Event Flow: btnStart.Click -> btnStart_Click shall be reviewable

The system SHALL make `Event Flow: btnStart.Click -> btnStart_Click` available as a workflow or UI-behavior review unit.

#### Scenario: Workflow chunk Event Flow: btnStart.Click -> btnStart_Click shall be reviewable

- GIVEN a maintainer reviews GUI behavior
- THEN chunk `0000_btnStart.Click_btnStart_Click` MUST provide source references and related chunks.

### Requirement: Workflow chunk Event Flow: btnSave.Click -> btnSave_Click shall be reviewable

The system SHALL make `Event Flow: btnSave.Click -> btnSave_Click` available as a workflow or UI-behavior review unit.

#### Scenario: Workflow chunk Event Flow: btnSave.Click -> btnSave_Click shall be reviewable

- GIVEN a maintainer reviews GUI behavior
- THEN chunk `0001_btnSave.Click_btnSave_Click` MUST provide source references and related chunks.

